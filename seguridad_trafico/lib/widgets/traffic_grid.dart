import 'package:flutter/material.dart';

class TrafficGrid extends StatelessWidget {
  const TrafficGrid({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('TrafficGrid - Empezando de cero'));
  }
}
