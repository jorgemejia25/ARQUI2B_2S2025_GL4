apiVersion: 1

providers:
  - name: "IoT Dashboards"
    orgId: 1
    folder: "IoT"
    folderUid: "iot-folder"
    type: file
    disableDeletion: true
    editable: false
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      # Make sure dashboards keep their UIDs from JSON
      foldersFromFilesStructure: false
